layout-macro-def (test-image-layout) {
  params {
    param (iconUri) {
      type (IconUri)
      min (Required) max (One)
    }
  }

  content {
    image {
      object-fit (Contain)
      url ("#{value(iconUri)}")
    }
  }
}