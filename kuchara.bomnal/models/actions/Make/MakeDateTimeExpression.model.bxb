action (MakeDateTimeExpression) {
  type(Search)
  description (__DESCRIPTION__)
  collect {
    input (dateTimeExpression) {
      type (time.DateTimeExpression)
      min (Optional) max (One)
    }
    input (detachedTime) {
      type (time.DetachedTime)
      min (Optional) max (One)
    }
    // 추가할 것
    
  }
  output (time.DateTimeExpression)
}
