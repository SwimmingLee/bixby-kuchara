action (FilterMovieWithGenre) {
  description (filter movie with genre input)
  type (Search)
  collect {
    input (movieSchedule) {
      type (MovieSchedule)
      min (Required) max (Many)
    }
    input (genre) {
      type (Genre)
      min (Required) max (One)
    }
    input(exceptExpression){
      type(ExceptExpression)
      min (Optional) max (One)
    }
  }
  output (MovieSchedule)
}