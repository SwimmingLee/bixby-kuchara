action (FindMovieWithTime) {
  description (__DESCRIPTION__)
  collect {
    input (dateTimeExpression) {
      type (time.DateTimeExpression)
      min (Required) max (One)
      default-init{
        intent {
          goal: MakeDateTimeExpression
        }
     }
    }
    input(inputTimeQuery) {
      type (InputTimeQuery)
      min (Required) max (One)
    }
  }
  output (DateTimeStructure)
}
